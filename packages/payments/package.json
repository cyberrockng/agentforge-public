{
  "name": "@agentforge/payments",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "preledger:check": "npm run build",
    "ledger:check": "node scripts/ledger-check.mjs",
    "preledger:postgres": "npm run build",
    "ledger:postgres": "node scripts/ledger-postgres.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10"
  }
}
